# Copy this file to .env and fill in values for your target environment.
# .env is gitignored — never commit real credentials.

# Application base URL (default: ParaBank demo site)
BASE_URL=https://parabank.parasoft.com
