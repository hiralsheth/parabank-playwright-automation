# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Test session state — generated by global-setup, must not be committed
/.auth/

# Local environment variables — copy from .env.example, never commit
.env
